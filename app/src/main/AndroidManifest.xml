<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tim.fypapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RegisterUserActivity" />
        <activity android:name=".SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".RegisterUserActivity" />
        <activity android:name=".TakeAttendanceActivity" />
        <activity android:name=".ViewClassesStudentStatsActivity" />
        <activity android:name=".AddStudentToClassActivity" />
        <activity android:name=".ViewClassesNewStudentActivity" />
        <activity android:name=".AddNewClassActivity" />
        <activity android:name=".ViewClassesTakeAttendanceActivity" />
        <activity android:name=".ViewClassesActivity" />
        <activity android:name=".StudentStatsActivity"></activity>
    </application>

</manifest>